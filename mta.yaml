_schema-version: "3.2"
ID: appmainapp
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: appmainapp-destination-content
  type: com.sap.application.content
  requires:
  - name: appmainapp-destination-service
    parameters:
      content-target: true
  - name: appmainapp-repo-host
    parameters:
      service-key:
        name: appmainapp-repo-host-key
  - name: appmainapp-uaa
    parameters:
      service-key:
        name: appmainapp-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: appmainapp_html_repo_host
          ServiceInstanceName: appmainapp-html5-service
          ServiceKeyName: appmainapp-repo-host-key
          sap.cloud.service: appmainapp
        - Authentication: OAuth2UserTokenExchange
          Name: appmainapp_uaa
          ServiceInstanceName: appmainapp-xsuaa-service
          ServiceKeyName: appmainapp-uaa-key
          sap.cloud.service: appmainapp
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: appmainapp-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: appmainapp-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - appmainapp.zip
      name: appmainapp
      target-path: resources/
- name: appmainapp
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: appmainapp-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: appmainapp-destination-service
    service-plan: lite
- name: appmainapp-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: appmainapp-xsuaa-service
    service-plan: application
- name: appmainapp-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: appmainapp-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
